[package]
name = "code-generator"
version = "0.1.0"
edition = "2021"

[dependencies]
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
anyhow = "1.0"
tokio = { version = "1.0", features = ["full"] }
handlebars = "5.0"
auto-observer = { path = "../auto-observer" }
visual-learner = { path = "../visual-learner" }
interaction-patterns = { path = "../interaction-patterns" }
tera = "1.19"
log = "0.4"

[dev-dependencies]
tokio-test = "0.4"
