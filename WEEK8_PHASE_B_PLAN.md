# Week 8 Phase B 实现计划 - 压力测试

**状态**: 🚀 准备执行
**预计时间**: 3 小时
**目标**: 验证系统在高负载下的稳定性和性能

---

## 🎯 Phase B 目标

### 主要目标
1. 验证系统在 100+ 并发请求下的稳定性
2. 监控内存和 CPU 资源使用情况
3. 识别性能瓶颈和限制
4. 确定系统的最大吞吐能力

### 成功标准
- ✅ 成功率 > 95% (在所有负载级别)
- ✅ 平均延迟 < 50ms (在 100 并发下)
- ✅ 内存稳定 (无泄漏，< 200MB)
- ✅ CPU 可控 (< 80% 平均)
- ✅ 无系统崩溃或挂起

---

## 📊 测试场景设计

### Test 1: Light Load (轻负载)
```
并发用户: 10
每用户请求: 10
总请求数: 100
渐增时间: 1秒
预期 RPS: 300-400
目标: 建立基线性能
```

### Test 2: Medium Load (中负载)
```
并发用户: 25
每用户请求: 10
总请求数: 250
渐增时间: 2秒
预期 RPS: 350-450
目标: 验证扩展能力
```

### Test 3: Heavy Load (重负载)
```
并发用户: 50
每用户请求: 10
总请求数: 500
渐增时间: 3秒
预期 RPS: 400-500
目标: 测试高并发处理
```

### Test 4: Extreme Load (极限负载)
```
并发用户: 100
每用户请求: 5
总请求数: 500
渐增时间: 5秒
超时: 15秒
预期 RPS: 300-400
目标: 发现系统极限
```

---

## 🔧 技术实现

### 1. 压力测试框架 (`stress_test.py`)

**核心组件**:

#### LoadTestConfig
```python
- concurrent_users: int      # 并发用户数
- requests_per_user: int     # 每用户请求数
- ramp_up_time: float        # 渐增时间
- timeout: float             # 请求超时
```

#### ResourceMonitor
```python
- 监控 CPU 使用率
- 监控内存占用
- 监控线程数
- 监控网络连接数
- 每秒采样一次
```

#### StressTestWorker
```python
- 多线程请求生成
- 真实 HTTP 请求
- 错误处理和重试
- 结果收集
```

#### StressTestRunner
```python
- 协调所有工作线程
- 资源监控管理
- 结果分析和统计
- 生成详细报告
```

### 2. 监控指标

**性能指标**:
- 总请求数
- 成功率 (%)
- 失败率 (%)
- 吞吐量 (RPS)
- 测试持续时间

**延迟指标**:
- 平均延迟
- 中位数延迟
- 最小/最大延迟
- 标准差
- P95/P99 百分位

**资源指标**:
- CPU 使用率 (平均/最大)
- 内存占用 (平均/最大)
- 线程数
- 网络连接数

**错误分析**:
- 错误类型统计
- 错误频率
- 错误发生时间

---

## 📈 执行流程

### 步骤 1: 环境准备
```bash
1. 激活虚拟环境
2. 安装依赖 (psutil, numpy)
3. 启动 API 服务器
4. 验证服务器健康
```

### 步骤 2: 渐进式负载测试
```bash
1. Test 1: 10 并发用户
2. 休息 2 秒
3. Test 2: 25 并发用户
4. 休息 2 秒
5. Test 3: 50 并发用户
6. 休息 2 秒
7. Test 4: 100 并发用户
```

### 步骤 3: 结果分析
```bash
1. 收集所有测试结果
2. 生成汇总报告
3. 对比不同负载级别
4. 识别性能趋势
5. 发现瓶颈和限制
```

### 步骤 4: 报告生成
```bash
1. 详细的 JSON 结果文件
2. 格式化的控制台输出
3. 性能对比表格
4. 执行报告文档
```

---

## 📊 预期结果

### 性能预期

| 负载级别 | 并发 | 请求数 | 预期成功率 | 预期延迟 | 预期 RPS |
|---------|------|--------|-----------|---------|----------|
| Light   | 10   | 100    | > 99%     | < 20ms  | 300-400  |
| Medium  | 25   | 250    | > 98%     | < 30ms  | 350-450  |
| Heavy   | 50   | 500    | > 96%     | < 40ms  | 400-500  |
| Extreme | 100  | 500    | > 95%     | < 50ms  | 300-400  |

### 资源预期

| 指标 | Light | Medium | Heavy | Extreme |
|------|-------|--------|-------|---------|
| CPU (平均) | < 20% | < 40% | < 60% | < 80% |
| CPU (最大) | < 40% | < 60% | < 80% | < 100% |
| 内存 (MB) | < 80  | < 120  | < 160  | < 200  |

---

## 🔍 关键验证点

### 1. 成功率验证
```
✅ Light Load: > 99%
✅ Medium Load: > 98%
✅ Heavy Load: > 96%
✅ Extreme Load: > 95%
```

### 2. 性能稳定性
```
✅ 延迟不随负载线性增长
✅ 标准差保持在合理范围
✅ P95/P99 在可接受范围
```

### 3. 资源管理
```
✅ 内存无泄漏 (测试后释放)
✅ CPU 不持续100%
✅ 线程数可控
✅ 连接正常关闭
```

### 4. 错误处理
```
✅ 超时错误 < 2%
✅ 连接错误 < 1%
✅ 服务器错误 < 2%
✅ 无崩溃或挂起
```

---

## 🛠️ 命令参考

### 快速执行
```bash
# 执行完整的 Phase B 测试套件
chmod +x run_phase_b_tests.sh
./run_phase_b_tests.sh
```

### 单独测试
```bash
# Light Load
python training/stress_test.py --users 10 --requests 10

# Medium Load
python training/stress_test.py --users 25 --requests 10

# Heavy Load
python training/stress_test.py --users 50 --requests 10

# Extreme Load
python training/stress_test.py --users 100 --requests 5 --timeout 15
```

### 自定义测试
```bash
python training/stress_test.py \
    --users 50 \
    --requests 20 \
    --ramp-up 3 \
    --timeout 10 \
    --output my_test.json
```

---

## 📝 输出文件

### JSON 结果文件
```
/tmp/stress_test_10users.json    - Light Load 结果
/tmp/stress_test_25users.json    - Medium Load 结果
/tmp/stress_test_50users.json    - Heavy Load 结果
/tmp/stress_test_100users.json   - Extreme Load 结果
```

### 服务器日志
```
/tmp/api_server_stress.log       - API 服务器运行日志
```

---

## 🚨 故障排查

### 问题 1: 高失败率
**症状**: 成功率 < 95%
**可能原因**:
- 服务器过载
- 超时设置太短
- 资源不足

**解决方案**:
1. 增加超时时间
2. 减少并发用户
3. 优化服务器性能

### 问题 2: 内存泄漏
**症状**: 内存持续增长
**可能原因**:
- 对象未释放
- 连接未关闭
- 缓存未清理

**解决方案**:
1. 检查资源管理代码
2. 确保连接正常关闭
3. 添加定期清理逻辑

### 问题 3: CPU 100%
**症状**: CPU 持续满载
**可能原因**:
- 计算密集操作
- 死循环
- 并发过高

**解决方案**:
1. 优化算法
2. 检查代码逻辑
3. 限制并发数

---

## 📋 下一步 (Phase C)

完成 Phase B 后，准备进行:

### Phase C: Docker 容器化
**目标**:
- 创建优化的 Docker 镜像
- 配置 docker-compose
- 容器化测试

**前置条件**:
- ✅ Phase B 通过 (成功率 > 95%)
- ✅ 性能瓶颈已识别
- ✅ 资源使用已优化

---

**Phase B 状态**: 🚀 准备执行
**预计完成时间**: 3 小时
**成功标准**: 明确定义 ✅
