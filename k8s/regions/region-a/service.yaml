apiVersion: v1
kind: Service
metadata:
  name: browerai-api
  namespace: browerai-region-a
  labels:
    app: browerai-api
    region: a
spec:
  type: ClusterIP
  selector:
    app: browerai-api
  ports:
  - name: http
    port: 80
    targetPort: 3000