# Training System Architecture & Diagrams

## 🏗️ 系统架构图

### 1. 完整数据流 (End-to-End Pipeline)

```
┌─────────────────────────────────────────────────────────────────────┐
│                    BrowerAI Training System                         │
└─────────────────────────────────────────────────────────────────────┘

┌─────────────────┐
│  原始网站数据    │  URLs + HTML + CSS + JS
│  (互联网)       │
└────────┬────────┘
         │
         ▼
┌─────────────────────────────────────────┐
│ 1️⃣ 数据采集 (Data Collection)         │
│ batch_crawl_websites.py                │
│ extract_website_complete.py            │
└────────┬────────────────────────────────┘
         │
         ▼ website_complete.jsonl
         │ (139-142 个网站)
         │
┌─────────────────────────────────────────┐
│ 2️⃣ 数据处理 (Data Preparation)        │
│ create_simplified_dataset.py            │
│ 原始网站 → 简化版本配对                  │
└────────┬────────────────────────────────┘
         │
         ▼ website_paired.jsonl
         │ (原始→简化配对)
         │
┌─────────────────────────────────────────┐
│ 3️⃣ 模型训练 (Model Training)          │
│ train_paired_website_generator.py       │
│                                         │
│ ┌───────────────────────────────────┐ │
│ │ Transformer Encoder-Decoder       │ │
│ │ d_model=256, nhead=8, layers=3   │ │
│ │ vocab_size=229 (字符级)          │ │
│ └───────────────────────────────────┘ │
│                                         │
│ 30 epochs × batch_size=2 = 2-3 小时   │
└────────┬────────────────────────────────┘
         │
         ▼ checkpoints/paired_generator/
         │ epoch_1.pt ~ epoch_30.pt
         │
┌─────────────────────────────────────────┐
│ 4️⃣ 模型导出 (Model Export)            │
│ export_to_onnx.py                      │
│ PyTorch → ONNX 格式                    │
└────────┬────────────────────────────────┘
         │
         ▼ models/local/
         │ website_generator_v1.onnx
         │
┌─────────────────────────────────────────┐
│ 5️⃣ Rust 集成 (Integration)            │
│ InferenceEngine::new(model_path)       │
│ 用于双渲染模式                          │
└─────────────────────────────────────────┘

输出: 简化优化的网站代码 ✓
```

---

## 📂 2. 目录结构树

```
training/                          (116 MB)
│
├─ 📊 data/                        (105 MB)
│  ├─ websites/
│  │  ├─ 1000_sites.jsonl         (142行, 110MB) ⭐ 大规模数据
│  │  ├─ quick_train.jsonl        (13行)
│  │  ├─ quick_val.jsonl          (2行)
│  │  ├─ demo_sample.jsonl        (1行)
│  │  ├─ depth_test.jsonl         (13行)
│  │  └─ verify_depth.jsonl       (13行)
│  ├─ website_complete.jsonl      (完整网站)
│  ├─ combined/                   (组合数据)
│  │  └─ full_pages/manifest.json
│  └─ *.txt                       (URL列表)
│
├─ 🧠 scripts/                     (396 KB, 3507 行)
│  ├─ batch_crawl_websites.py     (292行) - 爬取数据
│  ├─ extract_website_complete.py (142行) - 提取完整
│  ├─ create_simplified_dataset.py (246行) - 生成简化版 ⭐
│  ├─ train_paired_website_generator.py (246行) - 训练 ⭐
│  ├─ export_to_onnx.py           (182行) - 导出模型 ⭐
│  ├─ extract_features.py         (321行) - 特征提取
│  ├─ prepare_website_data.py     (668行) - 数据准备
│  ├─ dataset_manager.py          (474行) - 数据管理
│  ├─ collect_data.py             (497行) - 数据收集
│  └─ ...
│
├─ 🔧 core/                        (372 KB, 3116 行)
│  ├─ data/                       (409 行)
│  │  ├─ tokenizers.py            (400行) - 字符编码
│  │  └─ website_dataset.py       (389行) - PyTorch 数据集
│  ├─ models/                     (1200+ 行)
│  │  ├─ website_learner.py       (554行) - 主模型 ⭐
│  │  ├─ transformer.py           (?) - Transformer 架构
│  │  ├─ attention.py             (?) - 注意力机制
│  │  ├─ parsers.py               (267行) - 代码解析器
│  │  ├─ deobfuscator.py          (?) - 混淆器
│  │  └─ base.py                  (?) - 基础类
│  └─ trainers/                   (371 行)
│     └─ trainer.py               (360行) - 训练循环
│
├─ 🎯 configs/                     (20 KB)
│  ├─ website_learner.yaml        - 网站学习器配置 ⭐
│  ├─ website_learner_quick.yaml  - 快速版配置
│  ├─ deobfuscator.yaml           - 解混淆配置
│  └─ html_parser.yaml            - HTML解析配置
│
├─ 🧬 semantic_learning/           (172 KB)
│  ├─ *.log (15+ 日志文件)
│  └─ __pycache__/               (编译的模块)
│
├─ 📦 models/                      (8.8 MB)
│  ├─ site_classifier.pkl        - 网站分类器
│  ├─ code_understanding_v1.onnx.data
│  ├─ js_deobfuscator_v1.onnx.data
│  ├─ html_complexity_v1.onnx.data
│  └─ tokenizer_config.json
│
├─ 📋 features/                    (1.5 MB)
│  └─ extracted_features.jsonl   - 提取的特征
│
├─ 📝 logs/                        (672 KB)
│  └─ *.log (各种日志)
│
├─ 📚 Documentation
│  ├─ README.md                   (3.0 KB)
│  ├─ QUICKSTART.md               (5.6 KB)
│  ├─ WEBSITE_GENERATION_PLAN.md  (11 KB)
│  └─ CLEANUP_SUMMARY.md          (7.7 KB)
│
└─ ⚙️ Misc
   ├─ requirements.txt            - 依赖包
   ├─ venv/                       - 虚拟环境
   └─ *.log                       - 训练日志 (24个)
```

---

## 🔄 3. 数据流类型

### 3.1 JSONL 数据格式

```
website_complete.jsonl (输入)
├─ 行1: {"html": "...", "css": "...", "js": "...", "url": "..."}
├─ 行2: {"html": "...", "css": "...", "js": "...", "url": "..."}
└─ 行N: {...}
(139-142 行总计)
   │
   ▼ (create_simplified_dataset.py)
   │
website_paired.jsonl (输出)
├─ 行1: {
│    "html_original": "...",
│    "html_simplified": "...",  ← 简化版本 (72.95% 压缩率)
│    "css_original": "...",
│    "css_simplified": "...",
│    "js_original": "...",
│    "js_simplified": "..."
│  }
├─ 行2: {...}
└─ 行N: {...}
```

### 3.2 模型IO格式

```
输入层 (Input)
  ↓
原始网站代码字符
  ↓
Tokenizer (core/data/tokenizers.py)
  ↓
Token IDs (0-228)
  ↓
┌──────────────────────────────────┐
│ WebsiteLearner (Transformer)     │
│                                  │
│ Encoder:                         │
│ ├─ Embedding                     │
│ ├─ 3×TransformerEncoderLayer    │
│ └─ Output: context vector        │
│                                  │
│ Decoder:                         │
│ ├─ Embedding                     │
│ ├─ 3×TransformerDecoderLayer    │
│ └─ Linear: vocab_size=229        │
└──────────────────────────────────┘
  ↓
输出: 简化版本的 Token IDs
  ↓
Tokenizer.decode()
  ↓
输出层 (Output)
  ↓
简化优化的网站代码
```

---

## 🎯 4. 模块依赖关系

```
train_paired_website_generator.py (主脚本)
  │
  ├─ core.models.website_learner.WebsiteLearner
  │  ├─ core.models.transformer.Transformer
  │  │  ├─ core.models.attention.MultiHeadAttention
  │  │  └─ torch.nn modules
  │  ├─ core.models.base.BaseModel
  │  └─ torch optimization
  │
  ├─ core.data.website_dataset.WebsiteDataset
  │  ├─ core.data.tokenizers.Tokenizer
  │  └─ torch.utils.data.DataLoader
  │
  ├─ core.trainers.trainer.Trainer
  │  ├─ torch optimizers
  │  ├─ torch loss functions
  │  └─ evaluation metrics
  │
  └─ torch, numpy, pandas (基础依赖)

export_to_onnx.py
  ├─ core.models.website_learner.WebsiteLearner
  ├─ torch.onnx export
  └─ onnx validator

extract_features.py
  ├─ core.models.parsers
  │  ├─ HTMLParser
  │  ├─ CSSParser
  │  └─ JSParser
  ├─ beautifulsoup4
  └─ cssutils
```

---

## 📊 5. 训练过程可视化

```
Epoch 1                 Epoch 15               Epoch 30 (最终)
  ↓                       ↓                        ↓
┌─────────────┐      ┌─────────────┐         ┌─────────────┐
│ Loss: 5.2   │      │ Loss: 1.8   │         │ Loss: 0.45  │
│ BLEU: 0.15  │  →   │ BLEU: 0.52  │   →     │ BLEU: 0.70  │
│ Time: 4 min │      │ Time: 60min │         │ Time: 120min│
└─────────────┘      └─────────────┘         └─────────────┘

每个 Epoch:
1. 读取 batch (原始→简化配对)
2. 前向传播
3. 计算 Loss
4. 反向传播
5. 优化器更新
6. 保存检查点

Batch 内容: [HTML, CSS, JS] → [简化HTML, 简化CSS, 简化JS]
```

---

## 🔗 6. 与 Rust 核心的集成

```
┌─────────────────────────────────────┐
│  training/                          │
│  ├─ scripts/                        │
│  │  └─ export_to_onnx.py           │
│  ├─ models/local/                  │
│  │  ├─ website_generator_v1.onnx   │
│  │  └─ js_deobfuscator_v1.onnx     │
│  └─ core/models/                   │
│     └─ website_learner.py (参考)   │
└────────────┬────────────────────────┘
             │
             │ 导出 ONNX
             │
             ▼
┌─────────────────────────────────────────────────────────┐
│  src/ai/                                                │
│  ├─ inference.rs (ONNX Runtime)                        │
│  ├─ model_loader.rs (加载模型)                         │
│  ├─ model_manager.rs (模型管理)                        │
│  └─ integration.rs (集成各模型)                        │
│                                                         │
│  ┌─────────────────────────────────────┐             │
│  │ InferenceEngine                     │             │
│  ├─ load_model(onnx_path)             │             │
│  ├─ infer(input_tokens)               │             │
│  └─ hot_reload(new_model)             │             │
│  └─────────────────────────────────────┘             │
└─────────────────┬──────────────────────────────────────┘
                  │
                  │ 双渲染模式
                  │
                  ▼
        ┌─────────────────────┐
        │  原始网站代码       │
        │  vs                │
        │  AI 优化版本        │
        └─────────────────────┘
```

---

## 📈 7. 性能指标收集

```
训练指标 (per epoch):
├─ Loss (训练/验证)
├─ BLEU Score (相似度)
├─ ROUGE (覆盖率)
├─ 语法正确率
└─ 语义保留率

输出:
├─ training.log (详细)
├─ metrics.json (结构化)
└─ checkpoints/ (模型)

评估:
```
model = load_checkpoint('epoch_30.pt')
predictions = model.predict(test_set)

bleu = calculate_bleu(predictions, references)
rouge = calculate_rouge(predictions, references)
syntactic_correctness = check_syntax(predictions)
semantic_preservation = check_semantics(predictions)
compression_ratio = calculate_ratio(original, simplified)
```

---

## 🛠️ 8. 扩展点

### 8.1 新增数据源
```
新网站数据
  ↓
batch_crawl_websites.py
  ↓
data/websites/new_source.jsonl
  ↓
merge with existing data
```

### 8.2 模型改进
```
原模型: d_model=256, layers=3
  ↓
改进版: d_model=512, layers=6 (更大)
  ↓
修改 website_learner.py
  ↓
重新训练和导出
```

### 8.3 新的优化技术
```
知识蒸馏: 大模型 → 小模型
剪枝: 移除不重要权重
量化: 降低精度节省空间
```

---

## 🔐 9. 文件安全和版本管理

```
.gitignore 覆盖:
├─ data/websites/*.jsonl      ✓ (大数据文件)
├─ models/*.pth               ✓ (大模型文件)
├─ models/*.onnx              ✓ (导出模型)
├─ *.log                       ✓ (日志)
├─ checkpoints/               ✓ (训练检查点)
└─ __pycache__/               ✓ (编译缓存)

只提交到 Git:
├─ scripts/                   ✓ (源代码)
├─ core/                      ✓ (源代码)
├─ configs/                   ✓ (配置)
└─ 文档                        ✓ (文档)
```

---

## 📦 10. 部署流程

```
开发环境:
  training/ → 训练脚本 + 数据 + 检查点
  
  │
  └─→ export_to_onnx.py
      │
      └─→ website_generator.onnx

生产环境:
  models/local/website_generator.onnx
  │
  └─→ Rust InferenceEngine
      │
      └─→ BrowerAI 核心
          │
          └─→ 双渲染 (原始 vs AI优化)
              │
              └─→ 用户界面
```

---

## 🎓 11. 学习流程

```
初学者路径:
README.md (概览)
  ↓
QUICKSTART.md (教程)
  ↓
data/website_complete.jsonl (查看数据)
  ↓
scripts/ (运行脚本)
  ↓
checkpoint/ (查看结果)

开发者路径:
core/data/website_dataset.py (理解数据)
  ↓
core/models/website_learner.py (理解模型)
  ↓
core/trainers/trainer.py (理解训练)
  ↓
scripts/train_paired_website_generator.py (查看集成)

研究员路径:
WEBSITE_GENERATION_PLAN.md (架构)
  ↓
Transformer 论文复习
  ↓
改进模型架构
  ↓
实现新方法
  ↓
评估结果
```

---

**图表版本**: 1.0
**最后更新**: 2026-01-22
**维护者**: BrowerAI 文档团队
